apiVersion: v1
kind: Service
metadata:
  name: darkoperator-physics-validation-service
  namespace: darkoperator-ap-southeast-1
  labels:
    app: darkoperator
    service: physics-validation-service
spec:
  selector:
    app: darkoperator
    service: physics-validation-service
  ports:
    - 
      protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP